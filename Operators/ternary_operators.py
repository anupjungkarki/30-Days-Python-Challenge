# Ternary operators also known as conditional expression are operators that evaluate
# something based on a condition  being true or false.
# syntax: [on_true] if [expression] else [on_false]

a, b = 10, 20
min_value = a if a < b else b
print(min_value)
