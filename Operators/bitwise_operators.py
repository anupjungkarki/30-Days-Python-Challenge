# Binary operators are used to perform bit-level operations on (binary) numbers.
# the bitwise operators are:
# & (AND)
x = 0b1100
y = 0b1010
print(bin(x & y))
# we have other bitwise operators | ( or) , ^ (XOR) , ~ (NOT) , << (Left shift), >> (Right shift)
